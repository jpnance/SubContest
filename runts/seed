#!/bin/bash

if [ -z "$1" ]; then
	echo "Usage: runt seed <path-to-dump>"
	echo "Example: runt seed ~/backups/subcontest.dump"
	exit 1
fi

docker exec -i subcontest-mongo mongorestore --drop --archive < "$1"
